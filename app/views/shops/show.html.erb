<div class="jumbotron">
  <h1 class="display-4"><%= "#{@shop.shop_name} [#{@shop.plan}]" %></h1>
  <p class="lead">
    <%= "#{@shop.emp_first_name} #{@shop.emp_last_name}"%>
  </p>
  <p>
    <%= @shop.emp_email %>
  </p>
  <%= link_to 'Edit', edit_shop_path(@shop.id), class:"btn btn-secondary text-white" %>
  <%= link_to 'Back', shops_path, class:"btn btn-dark text-white" %>
</div>
